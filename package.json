{
    "name": "spec-explorer",
    "private": true,
    "scripts": {
        "watch": "concurrently \"pnpm --filter @spec-explorer/library run watch\" \"pnpm --filter unit-tests run watch\"",
        "test": "pnpm --filter unit-tests run test",
        "test-watch": "nodemon --watch lib/dist --watch test/unit/dist --ext js --exec \"pnpm test\"",
        "clean": "pnpm --filter @spec-explorer/library run clean && pnpm --filter unit-tests run clean && pnpm --filter @spec-explorer/vscode run clean",
        "build": "pnpm --filter @spec-explorer/library run build && pnpm --filter unit-tests run build && pnpm --filter @spec-explorer/vscode run build",
        "clean-build": "pnpm run clean && pnpm run build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "nodemon": "^3.0.3"
    }
}