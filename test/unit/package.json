{
  "name": "unit-tests",
  "version": "1.0.0",
  "description": "...",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rd /s /q dist",
    "clean-build": "pnpm run clean && pnpm run build",
    "test": "ts-mocha --require ts-node/esm src/**/*.spec.ts",
    "test-watch": "ts-mocha --require ts-node/esm --watch src/**/*.spec.ts"
  },
  "dependencies": {
    "@spec-explorer/library": "workspace:*"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.5",
    "chai": "4.4.1",
    "mocha": "^10.2.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}